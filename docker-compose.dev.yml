services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cic
      POSTGRES_PASSWORD: cic
      POSTGRES_DB: cic
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data

volumes:
  postgres_dev_data:
